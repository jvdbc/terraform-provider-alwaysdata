{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Terraform Provider",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            // this assumes your workspace is the root of the repo
            "program": "${workspaceFolder}",
            // "program": "${fileDirname}"
            "env": {
                "TF_LOG": "DEBUG",
                "TF_LOG_PATH": "/Users/jvasdeboncoeur/Workdir/jvdbc/terraform-provider-alwaysdata/build/terraform-debug.log"
            },
            "args": [
                "-debug",
            ]
        }
    ]
}
